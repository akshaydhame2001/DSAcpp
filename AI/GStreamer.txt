gst-launch-1.0 [source] ! [decoder] ! [processing] ! [sink]

nvarguscamerasrc	Access camera (CSI camera)
nvvidconv	        Hardware-accelerated color conversion / scaling
nvv4l2decoder	    Hardware-accelerated video decoding
nvv4l2h264enc / nvv4l2h265enc	Hardware-accelerated video encoding
nvjpegenc / nvjpegdec	JPEG encoding / decoding
nvegltransform	    Transformation for EGL rendering
nveglglessink	    Efficient rendering to screen

CSI Camera pipeline
gst-launch-1.0 nvarguscamerasrc ! \
'video/x-raw(memory:NVMM), width=1280, height=720, framerate=30/1' ! \
nvvidconv ! nvegltransform ! nveglglessink -e

USB camera (OpenCV)
import cv2
gst_str = (
    "v4l2src device=/dev/video0 ! "
    "video/x-raw, width=640, height=480, framerate=30/1 ! "
    "videoconvert ! appsink"
)
cap = cv2.VideoCapture(gst_str, cv2.CAP_GSTREAMER)

Stream RTSP and decoder
gst-launch-1.0 rtspsrc location=rtsp://IP:PORT ! \
rtph264depay ! h264parse ! nvv4l2decoder ! \
nvvidconv ! nvegltransform ! nveglglessink

Check supported camera formats	v4l2-ctl --list-formats-ext
Show GStreamer version	        gst-inspect-1.0 --version
List all GStreamer plugins	    gst-inspect-1.0
Debug pipeline issues	        Add GST_DEBUG=3 before launch
